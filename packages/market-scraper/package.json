{
  "name": "@trusaas/market-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Market-agnostic valuation pipeline for TruSaaS (cars + housing, any country).",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "test": "tsx scripts/test-markets.ts",
    "webapp": "tsx webapp/server.ts",
    "build:webapp": "esbuild webapp/server.ts --bundle --platform=node --format=cjs --packages=external --outfile=webapp/server.cjs",
    "start:webapp": "node webapp/server.cjs"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "esbuild": "^0.24.0",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.19.0"
  }
}
